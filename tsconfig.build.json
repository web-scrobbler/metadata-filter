// This config is used for build only
{
  "compilerOptions": {
    "declaration": true,
    "declarationDir": "types",
    "outDir": "dist",
    "target": "ES2015",
    "strict": true
  },
  // Skip `test` directory to prevent generating declaration files for tests
  "include": ["src/**/*"]
}
